1. // This program calculates the product of three integers
// Declare variables of type int
int x, y, z, result;
// Prompt user to enter three integers
cout << "Please enter three integers: ";
// Read three integers from keyboard and store them in x, y, and z
cin >> x >> y >> z;
// Compute the product of x, y, and z and assign the result to variable result
result = x * y * z;
 // Print the product 
 cout << "The product is " << result << endl;
 // Return a value from main indicating that the program terminated successfully 
return 0;
